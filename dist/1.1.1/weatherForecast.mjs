var img$2 = "data:image/png;base64,";

var img$1 = "data:image/svg+xml,%3c%3fxml version='1.0' encoding='UTF-8'%3f%3e%3csvg id='_%e3%83%ac%e3%82%a4%e3%83%a4%e3%83%bc_1' data-name='%e3%83%ac%e3%82%a4%e3%83%a4%e3%83%bc_1' xmlns='http://www.w3.org/2000/svg' version='1.1' viewBox='0 0 80 80'%3e %3c!-- Generator: Adobe Illustrator 30.2.1%2c SVG Export Plug-In . SVG Version: 2.1.1 Build 1) --%3e %3cdefs%3e %3cstyle%3e .st0 %7b fill: white%3b %7d .st1 %7b fill: %234d4d4d%3b %7d %3c/style%3e %3c/defs%3e %3cg%3e %3cpath class='st0' d='M67.5778%2c55.7257c-.4313-3.2704-3.2232-5.7967-6.6113-5.7967-.743%2c0-1.4552.1265-2.1227.3505-.9347-4.3781-4.8233-7.662-9.4803-7.662-3.9327%2c0-7.316%2c2.3428-8.837%2c5.7076-.9972-.5927-2.1577-.9393-3.4019-.9393-3.6869%2c0-6.6757%2c2.9888-6.6757%2c6.6757%2c0%2c.5488.0735%2c1.0792.1984%2c1.5895h-.0394c-4.1258%2c0-7.4705%2c3.3446-7.4705%2c7.4705s3.3446%2c7.4705%2c7.4705%2c7.4705h35.9217c4.1258%2c0%2c7.4705-3.3446%2c7.4705-7.4705%2c0-3.7699-2.793-6.8859-6.4222-7.3958Z'/%3e %3cpath class='st1' d='M66.5293%2c71.584H30.6079c-4.6665%2c0-8.4629-3.7969-8.4629-8.4629%2c0-4.2861%2c3.2041-7.8389%2c7.3433-8.3877-.021-.2266-.0317-.4502-.0317-.6719%2c0-4.2285%2c3.4399-7.668%2c7.668-7.668%2c1.0195%2c0%2c2.0293.207%2c2.9683.6035%2c1.8926-3.2939%2c5.4277-5.3721%2c9.2705-5.3721%2c4.6621%2c0%2c8.792%2c3.0752%2c10.1855%2c7.4463.4688-.0898.9434-.1348%2c1.418-.1348%2c3.582%2c0%2c6.6719%2c2.5068%2c7.4648%2c5.9385%2c3.7871.873%2c6.5605%2c4.2969%2c6.5605%2c8.2461%2c0%2c4.666-3.7969%2c8.4629-8.4629%2c8.4629ZM30.6475%2c56.6436c-3.6118%2c0-6.5181%2c2.9062-6.5181%2c6.4775s2.9062%2c6.4785%2c6.4785%2c6.4785h35.9214c3.5723%2c0%2c6.4785-2.9062%2c6.4785-6.4785%2c0-3.21-2.3936-5.9668-5.5674-6.4131l-.7471-.1045-.0986-.748c-.3711-2.8135-2.791-4.9346-5.6279-4.9346-.6064%2c0-1.2148.1006-1.8076.2988l-1.0537.3525-.2324-1.0859c-.8506-3.9844-4.4297-6.877-8.5098-6.877-3.4111%2c0-6.5254%2c2.0117-7.9326%2c5.125l-.4575%2c1.0107-.9536-.5674c-.8809-.5234-1.8818-.7998-2.895-.7998-3.1338%2c0-5.6836%2c2.5498-5.6836%2c5.6836%2c0%2c.4434.0557.8857.1704%2c1.3535l.3003%2c1.2285h-1.2642Z'/%3e %3c/g%3e %3cg%3e %3ccircle class='st1' cx='30.2103' cy='31.9317' r='11.9275'/%3e %3cpath class='st1' d='M52.7665%2c33.414h-6.3344c-.8881%2c0-1.6077-.7201-1.6077-1.6082s.7196-1.6082%2c1.6077-1.6082h6.3344c.8881%2c0%2c1.6077.7201%2c1.6077%2c1.6082s-.7196%2c1.6082-1.6077%2c1.6082Z'/%3e %3cpath class='st1' d='M41.7279%2c21.829c-.4113%2c0-.823-.1569-1.1369-.4708-.6278-.6287-.6278-1.646%2c0-2.2747l4.4793-4.4793c.6278-.6278%2c1.646-.6278%2c2.2738%2c0%2c.6278.6287.6278%2c1.646%2c0%2c2.2747l-4.4793%2c4.4793c-.3139.3139-.7256.4708-1.1369.4708Z'/%3e %3cpath class='st1' d='M30.2103%2c16.9638c-.8881%2c0-1.6077-.7201-1.6077-1.6082v-6.3349c0-.8881.7196-1.6082%2c1.6077-1.6082s1.6077.7201%2c1.6077%2c1.6082v6.3349c0%2c.8881-.7196%2c1.6082-1.6077%2c1.6082Z'/%3e %3cpath class='st1' d='M18.6257%2c21.6674c-.4113%2c0-.823-.1569-1.1369-.4708l-4.4793-4.4793c-.6278-.6287-.6278-1.646%2c0-2.2747.6278-.6278%2c1.646-.6278%2c2.2738%2c0l4.4793%2c4.4793c.6278.6287.6278%2c1.646%2c0%2c2.2747-.3139.3139-.7256.4708-1.1369.4708Z'/%3e %3cpath class='st1' d='M13.7605%2c33.1851h-6.3344c-.8881%2c0-1.6077-.7201-1.6077-1.6082s.7196-1.6082%2c1.6077-1.6082h6.3344c.8881%2c0%2c1.6077.7201%2c1.6077%2c1.6082s-.7196%2c1.6082-1.6077%2c1.6082Z'/%3e %3cpath class='st1' d='M13.9853%2c49.2494c-.4113%2c0-.823-.1569-1.1369-.4708-.6278-.6287-.6278-1.646%2c0-2.2747l4.4793-4.4793c.6278-.6278%2c1.646-.6278%2c2.2738%2c0%2c.6278.6287.6278%2c1.646%2c0%2c2.2747l-4.4793%2c4.4793c-.3139.3139-.7256.4708-1.1369.4708Z'/%3e %3c/g%3e%3c/svg%3e";

var en$1 = {
	"weatherForecast.entry.name": "Weather Forecast",
	"weatherForecast.entry.description": "Get a weather forecast (temperature, precipitation, weather, wind) by Japanese postal code using Open-Meteo."
};
var ja$1 = {
	"weatherForecast.entry.name": "天気予報",
	"weatherForecast.entry.description": "郵便番号からn時間後の気温・降水確率・天気・風速を取得します（Open-Meteo を使用）。"
};
var translations$1 = {
	en: en$1,
	ja: ja$1,
	"ja-Hira": {
	"weatherForecast.entry.name": "てんきよほう",
	"weatherForecast.entry.description": "ゆうびんばんごうから nじかんごの きおん・こうすいかくりつ・てんき・ふうそくを とりだします。"
}
};

var version$1 = "1.1.1";

/**
 * This is an extension for Xcratch.
 */


/**
 * Formatter to translate the messages in this extension.
 * This will be replaced which is used in the React component.
 * @param {object} messageData - data for format-message
 * @returns {string} - translated message for the current locale
 */
var formatMessage$1 = function formatMessage(messageData) {
  return messageData.defaultMessage;
};
var version = "v".concat(version$1);
var entry = {
  get name() {
    return formatMessage$1({
      id: 'weatherForecast.entry.name',
      defaultMessage: '天気予報',
      description: 'name of the extension'
    });
  },
  extensionId: 'weatherForecast',
  extensionURL: 'https://asondemita.github.io/xcx-weather/dist/weatherForecast.mjs',
  collaborator: 'asondemita',
  iconURL: img$2,
  insetIconURL: img$1,
  get description() {
    return "".concat(formatMessage$1({
      defaultMessage: '郵便番号からn時間後の気温・降水確率・天気・風速を取得します（Open-Meteo を使用）。',
      description: 'Description for this extension',
      id: 'weatherForecast.entry.description'
    }), " (").concat(version, ")");
  },
  tags: [],
  featured: true,
  disabled: false,
  bluetoothRequired: false,
  internetConnectionRequired: true,
  helpLink: 'https://asondemita.github.io/xcx-weather/',
  setFormatMessage: function setFormatMessage(formatter) {
    formatMessage$1 = formatter;
  },
  translationMap: translations$1
};

function _classCallCheck(a, n) {
  if (!(a instanceof n)) throw new TypeError("Cannot call a class as a function");
}

function _typeof(o) {
  "@babel/helpers - typeof";

  return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) {
    return typeof o;
  } : function (o) {
    return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o;
  }, _typeof(o);
}

function toPrimitive(t, r) {
  if ("object" != _typeof(t) || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i = e.call(t, r);
    if ("object" != _typeof(i)) return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (String )(t);
}

function toPropertyKey(t) {
  var i = toPrimitive(t, "string");
  return "symbol" == _typeof(i) ? i : i + "";
}

function _defineProperties(e, r) {
  for (var t = 0; t < r.length; t++) {
    var o = r[t];
    o.enumerable = o.enumerable || false, o.configurable = true, "value" in o && (o.writable = true), Object.defineProperty(e, toPropertyKey(o.key), o);
  }
}
function _createClass(e, r, t) {
  return r && _defineProperties(e.prototype, r), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", {
    writable: false
  }), e;
}

function getDefaultExportFromCjs (x) {
	return x && x.__esModule && Object.prototype.hasOwnProperty.call(x, 'default') ? x['default'] : x;
}

/**
 * Types of block
 * @enum {string}
 */
var blockType;
var hasRequiredBlockType;
function requireBlockType() {
  if (hasRequiredBlockType) return blockType;
  hasRequiredBlockType = 1;
  var BlockType = {
    /**
     * Boolean reporter with hexagonal shape
     */
    BOOLEAN: 'Boolean',
    /**
     * A button (not an actual block) for some special action, like making a variable
     */
    BUTTON: 'button',
    /**
     * Command block
     */
    COMMAND: 'command',
    /**
     * Specialized command block which may or may not run a child branch
     * The thread continues with the next block whether or not a child branch ran.
     */
    CONDITIONAL: 'conditional',
    /**
     * Specialized hat block with no implementation function
     * This stack only runs if the corresponding event is emitted by other code.
     */
    EVENT: 'event',
    /**
     * Hat block which conditionally starts a block stack
     */
    HAT: 'hat',
    /**
     * Specialized command block which may or may not run a child branch
     * If a child branch runs, the thread evaluates the loop block again.
     */
    LOOP: 'loop',
    /**
     * General reporter with numeric or string value
     */
    REPORTER: 'reporter'
  };
  blockType = BlockType;
  return blockType;
}

var blockTypeExports = requireBlockType();
var BlockType = /*@__PURE__*/getDefaultExportFromCjs(blockTypeExports);

/**
 * Block argument types
 * @enum {string}
 */
var argumentType;
var hasRequiredArgumentType;
function requireArgumentType() {
  if (hasRequiredArgumentType) return argumentType;
  hasRequiredArgumentType = 1;
  var ArgumentType = {
    /**
     * Numeric value with angle picker
     */
    ANGLE: 'angle',
    /**
     * Boolean value with hexagonal placeholder
     */
    BOOLEAN: 'Boolean',
    /**
     * Numeric value with color picker
     */
    COLOR: 'color',
    /**
     * Numeric value with text field
     */
    NUMBER: 'number',
    /**
     * String value with text field
     */
    STRING: 'string',
    /**
     * String value with matrix field
     */
    MATRIX: 'matrix',
    /**
     * MIDI note number with note picker (piano) field
     */
    NOTE: 'note',
    /**
     * Inline image on block (as part of the label)
     */
    IMAGE: 'image'
  };
  argumentType = ArgumentType;
  return argumentType;
}

var argumentTypeExports = requireArgumentType();
var ArgumentType = /*@__PURE__*/getDefaultExportFromCjs(argumentTypeExports);

var color;
var hasRequiredColor;
function requireColor() {
  if (hasRequiredColor) return color;
  hasRequiredColor = 1;
  var Color = /*#__PURE__*/function () {
    function Color() {
      _classCallCheck(this, Color);
    }
    return _createClass(Color, null, [{
      key: "RGB_BLACK",
      get:
      /**
       * @typedef {object} RGBObject - An object representing a color in RGB format.
       * @property {number} r - the red component, in the range [0, 255].
       * @property {number} g - the green component, in the range [0, 255].
       * @property {number} b - the blue component, in the range [0, 255].
       */

      /**
       * @typedef {object} HSVObject - An object representing a color in HSV format.
       * @property {number} h - hue, in the range [0-359).
       * @property {number} s - saturation, in the range [0,1].
       * @property {number} v - value, in the range [0,1].
       */

      /** @type {RGBObject} */
      function get() {
        return {
          r: 0,
          g: 0,
          b: 0
        };
      }

      /** @type {RGBObject} */
    }, {
      key: "RGB_WHITE",
      get: function get() {
        return {
          r: 255,
          g: 255,
          b: 255
        };
      }

      /**
       * Convert a Scratch decimal color to a hex string, #RRGGBB.
       * @param {number} decimal RGB color as a decimal.
       * @return {string} RGB color as #RRGGBB hex string.
       */
    }, {
      key: "decimalToHex",
      value: function decimalToHex(decimal) {
        if (decimal < 0) {
          decimal += 0xFFFFFF + 1;
        }
        var hex = Number(decimal).toString(16);
        hex = "#".concat('000000'.substr(0, 6 - hex.length)).concat(hex);
        return hex;
      }

      /**
       * Convert a Scratch decimal color to an RGB color object.
       * @param {number} decimal RGB color as decimal.
       * @return {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       */
    }, {
      key: "decimalToRgb",
      value: function decimalToRgb(decimal) {
        var a = decimal >> 24 & 0xFF;
        var r = decimal >> 16 & 0xFF;
        var g = decimal >> 8 & 0xFF;
        var b = decimal & 0xFF;
        return {
          r: r,
          g: g,
          b: b,
          a: a > 0 ? a : 255
        };
      }

      /**
       * Convert a hex color (e.g., F00, #03F, #0033FF) to an RGB color object.
       * CC-BY-SA Tim Down:
       * https://stackoverflow.com/questions/5623838/rgb-to-hex-and-hex-to-rgb
       * @param {!string} hex Hex representation of the color.
       * @return {RGBObject} null on failure, or rgb: {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       */
    }, {
      key: "hexToRgb",
      value: function hexToRgb(hex) {
        var shorthandRegex = /^#?([a-f\d])([a-f\d])([a-f\d])$/i;
        hex = hex.replace(shorthandRegex, function (m, r, g, b) {
          return r + r + g + g + b + b;
        });
        var result = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
        return result ? {
          r: parseInt(result[1], 16),
          g: parseInt(result[2], 16),
          b: parseInt(result[3], 16)
        } : null;
      }

      /**
       * Convert an RGB color object to a hex color.
       * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       * @return {!string} Hex representation of the color.
       */
    }, {
      key: "rgbToHex",
      value: function rgbToHex(rgb) {
        return Color.decimalToHex(Color.rgbToDecimal(rgb));
      }

      /**
       * Convert an RGB color object to a Scratch decimal color.
       * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       * @return {!number} Number representing the color.
       */
    }, {
      key: "rgbToDecimal",
      value: function rgbToDecimal(rgb) {
        return (rgb.r << 16) + (rgb.g << 8) + rgb.b;
      }

      /**
      * Convert a hex color (e.g., F00, #03F, #0033FF) to a decimal color number.
      * @param {!string} hex Hex representation of the color.
      * @return {!number} Number representing the color.
      */
    }, {
      key: "hexToDecimal",
      value: function hexToDecimal(hex) {
        return Color.rgbToDecimal(Color.hexToRgb(hex));
      }

      /**
       * Convert an HSV color to RGB format.
       * @param {HSVObject} hsv - {h: hue [0,360), s: saturation [0,1], v: value [0,1]}
       * @return {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       */
    }, {
      key: "hsvToRgb",
      value: function hsvToRgb(hsv) {
        var h = hsv.h % 360;
        if (h < 0) h += 360;
        var s = Math.max(0, Math.min(hsv.s, 1));
        var v = Math.max(0, Math.min(hsv.v, 1));
        var i = Math.floor(h / 60);
        var f = h / 60 - i;
        var p = v * (1 - s);
        var q = v * (1 - s * f);
        var t = v * (1 - s * (1 - f));
        var r;
        var g;
        var b;
        switch (i) {
          default:
          case 0:
            r = v;
            g = t;
            b = p;
            break;
          case 1:
            r = q;
            g = v;
            b = p;
            break;
          case 2:
            r = p;
            g = v;
            b = t;
            break;
          case 3:
            r = p;
            g = q;
            b = v;
            break;
          case 4:
            r = t;
            g = p;
            b = v;
            break;
          case 5:
            r = v;
            g = p;
            b = q;
            break;
        }
        return {
          r: Math.floor(r * 255),
          g: Math.floor(g * 255),
          b: Math.floor(b * 255)
        };
      }

      /**
       * Convert an RGB color to HSV format.
       * @param {RGBObject} rgb - {r: red [0,255], g: green [0,255], b: blue [0,255]}.
       * @return {HSVObject} hsv - {h: hue [0,360), s: saturation [0,1], v: value [0,1]}
       */
    }, {
      key: "rgbToHsv",
      value: function rgbToHsv(rgb) {
        var r = rgb.r / 255;
        var g = rgb.g / 255;
        var b = rgb.b / 255;
        var x = Math.min(Math.min(r, g), b);
        var v = Math.max(Math.max(r, g), b);

        // For grays, hue will be arbitrarily reported as zero. Otherwise, calculate
        var h = 0;
        var s = 0;
        if (x !== v) {
          var f = r === x ? g - b : g === x ? b - r : r - g;
          var i = r === x ? 3 : g === x ? 5 : 1;
          h = (i - f / (v - x)) * 60 % 360;
          s = (v - x) / v;
        }
        return {
          h: h,
          s: s,
          v: v
        };
      }

      /**
       * Linear interpolation between rgb0 and rgb1.
       * @param {RGBObject} rgb0 - the color corresponding to fraction1 <= 0.
       * @param {RGBObject} rgb1 - the color corresponding to fraction1 >= 1.
       * @param {number} fraction1 - the interpolation parameter. If this is 0.5, for example, mix the two colors equally.
       * @return {RGBObject} the interpolated color.
       */
    }, {
      key: "mixRgb",
      value: function mixRgb(rgb0, rgb1, fraction1) {
        if (fraction1 <= 0) return rgb0;
        if (fraction1 >= 1) return rgb1;
        var fraction0 = 1 - fraction1;
        return {
          r: fraction0 * rgb0.r + fraction1 * rgb1.r,
          g: fraction0 * rgb0.g + fraction1 * rgb1.g,
          b: fraction0 * rgb0.b + fraction1 * rgb1.b
        };
      }
    }]);
  }();
  color = Color;
  return color;
}

var cast;
var hasRequiredCast;
function requireCast() {
  if (hasRequiredCast) return cast;
  hasRequiredCast = 1;
  var Color = requireColor();

  /**
   * @fileoverview
   * Utilities for casting and comparing Scratch data-types.
   * Scratch behaves slightly differently from JavaScript in many respects,
   * and these differences should be encapsulated below.
   * For example, in Scratch, add(1, join("hello", world")) -> 1.
   * This is because "hello world" is cast to 0.
   * In JavaScript, 1 + Number("hello" + "world") would give you NaN.
   * Use when coercing a value before computation.
   */
  var Cast = /*#__PURE__*/function () {
    function Cast() {
      _classCallCheck(this, Cast);
    }
    return _createClass(Cast, null, [{
      key: "toNumber",
      value:
      /**
       * Scratch cast to number.
       * Treats NaN as 0.
       * In Scratch 2.0, this is captured by `interp.numArg.`
       * @param {*} value Value to cast to number.
       * @return {number} The Scratch-casted number value.
       */
      function toNumber(value) {
        // If value is already a number we don't need to coerce it with
        // Number().
        if (typeof value === 'number') {
          // Scratch treats NaN as 0, when needed as a number.
          // E.g., 0 + NaN -> 0.
          if (Number.isNaN(value)) {
            return 0;
          }
          return value;
        }
        var n = Number(value);
        if (Number.isNaN(n)) {
          // Scratch treats NaN as 0, when needed as a number.
          // E.g., 0 + NaN -> 0.
          return 0;
        }
        return n;
      }

      /**
       * Scratch cast to boolean.
       * In Scratch 2.0, this is captured by `interp.boolArg.`
       * Treats some string values differently from JavaScript.
       * @param {*} value Value to cast to boolean.
       * @return {boolean} The Scratch-casted boolean value.
       */
    }, {
      key: "toBoolean",
      value: function toBoolean(value) {
        // Already a boolean?
        if (typeof value === 'boolean') {
          return value;
        }
        if (typeof value === 'string') {
          // These specific strings are treated as false in Scratch.
          if (value === '' || value === '0' || value.toLowerCase() === 'false') {
            return false;
          }
          // All other strings treated as true.
          return true;
        }
        // Coerce other values and numbers.
        return Boolean(value);
      }

      /**
       * Scratch cast to string.
       * @param {*} value Value to cast to string.
       * @return {string} The Scratch-casted string value.
       */
    }, {
      key: "toString",
      value: function toString(value) {
        return String(value);
      }

      /**
       * Cast any Scratch argument to an RGB color array to be used for the renderer.
       * @param {*} value Value to convert to RGB color array.
       * @return {Array.<number>} [r,g,b], values between 0-255.
       */
    }, {
      key: "toRgbColorList",
      value: function toRgbColorList(value) {
        var color = Cast.toRgbColorObject(value);
        return [color.r, color.g, color.b];
      }

      /**
       * Cast any Scratch argument to an RGB color object to be used for the renderer.
       * @param {*} value Value to convert to RGB color object.
       * @return {RGBOject} [r,g,b], values between 0-255.
       */
    }, {
      key: "toRgbColorObject",
      value: function toRgbColorObject(value) {
        var color;
        if (typeof value === 'string' && value.substring(0, 1) === '#') {
          color = Color.hexToRgb(value);

          // If the color wasn't *actually* a hex color, cast to black
          if (!color) color = {
            r: 0,
            g: 0,
            b: 0,
            a: 255
          };
        } else {
          color = Color.decimalToRgb(Cast.toNumber(value));
        }
        return color;
      }

      /**
       * Determine if a Scratch argument is a white space string (or null / empty).
       * @param {*} val value to check.
       * @return {boolean} True if the argument is all white spaces or null / empty.
       */
    }, {
      key: "isWhiteSpace",
      value: function isWhiteSpace(val) {
        return val === null || typeof val === 'string' && val.trim().length === 0;
      }

      /**
       * Compare two values, using Scratch cast, case-insensitive string compare, etc.
       * In Scratch 2.0, this is captured by `interp.compare.`
       * @param {*} v1 First value to compare.
       * @param {*} v2 Second value to compare.
       * @returns {number} Negative number if v1 < v2; 0 if equal; positive otherwise.
       */
    }, {
      key: "compare",
      value: function compare(v1, v2) {
        var n1 = Number(v1);
        var n2 = Number(v2);
        if (n1 === 0 && Cast.isWhiteSpace(v1)) {
          n1 = NaN;
        } else if (n2 === 0 && Cast.isWhiteSpace(v2)) {
          n2 = NaN;
        }
        if (isNaN(n1) || isNaN(n2)) {
          // At least one argument can't be converted to a number.
          // Scratch compares strings as case insensitive.
          var s1 = String(v1).toLowerCase();
          var s2 = String(v2).toLowerCase();
          if (s1 < s2) {
            return -1;
          } else if (s1 > s2) {
            return 1;
          }
          return 0;
        }
        // Handle the special case of Infinity
        if (n1 === Infinity && n2 === Infinity || n1 === -Infinity && n2 === -Infinity) {
          return 0;
        }
        // Compare as numbers.
        return n1 - n2;
      }

      /**
       * Determine if a Scratch argument number represents a round integer.
       * @param {*} val Value to check.
       * @return {boolean} True if number looks like an integer.
       */
    }, {
      key: "isInt",
      value: function isInt(val) {
        // Values that are already numbers.
        if (typeof val === 'number') {
          if (isNaN(val)) {
            // NaN is considered an integer.
            return true;
          }
          // True if it's "round" (e.g., 2.0 and 2).
          return val === parseInt(val, 10);
        } else if (typeof val === 'boolean') {
          // `True` and `false` always represent integer after Scratch cast.
          return true;
        } else if (typeof val === 'string') {
          // If it contains a decimal point, don't consider it an int.
          return val.indexOf('.') < 0;
        }
        return false;
      }
    }, {
      key: "LIST_INVALID",
      get: function get() {
        return 'INVALID';
      }
    }, {
      key: "LIST_ALL",
      get: function get() {
        return 'ALL';
      }

      /**
       * Compute a 1-based index into a list, based on a Scratch argument.
       * Two special cases may be returned:
       * LIST_ALL: if the block is referring to all of the items in the list.
       * LIST_INVALID: if the index was invalid in any way.
       * @param {*} index Scratch arg, including 1-based numbers or special cases.
       * @param {number} length Length of the list.
       * @param {boolean} acceptAll Whether it should accept "all" or not.
       * @return {(number|string)} 1-based index for list, LIST_ALL, or LIST_INVALID.
       */
    }, {
      key: "toListIndex",
      value: function toListIndex(index, length, acceptAll) {
        if (typeof index !== 'number') {
          if (index === 'all') {
            return acceptAll ? Cast.LIST_ALL : Cast.LIST_INVALID;
          }
          if (index === 'last') {
            if (length > 0) {
              return length;
            }
            return Cast.LIST_INVALID;
          } else if (index === 'random' || index === 'any') {
            if (length > 0) {
              return 1 + Math.floor(Math.random() * length);
            }
            return Cast.LIST_INVALID;
          }
        }
        index = Math.floor(Cast.toNumber(index));
        if (index < 1 || index > length) {
          return Cast.LIST_INVALID;
        }
        return index;
      }
    }]);
  }();
  cast = Cast;
  return cast;
}

var castExports = requireCast();
var Cast = /*@__PURE__*/getDefaultExportFromCjs(castExports);

var en = {
	"weatherForecast.name": "Weather Forecast",
	"weatherForecast.getForecast": "forecast [ITEM] in [HOURS] hours near zip [ZIP]",
	"weatherForecast.getDailyForecast": "weekly [DAILY_ITEM] in [DAY] days near zip [ZIP]",
	"weatherForecast.getPlaceName": "place name of zip [ZIP]",
	"weatherForecast.item.temperature": "temperature",
	"weatherForecast.item.precipitation": "precipitation probability",
	"weatherForecast.item.weather": "weather",
	"weatherForecast.item.windspeed": "wind speed",
	"weatherForecast.item.winddir": "wind direction",
	"weatherForecast.item.wbgt": "heat index (WBGT)",
	"weatherForecast.item.wbgtLevel": "heat risk level (WBGT)",
	"weatherForecast.daily.weather": "weather",
	"weatherForecast.daily.tempMax": "highest temperature",
	"weatherForecast.daily.tempMin": "lowest temperature",
	"weatherForecast.daily.precipitation": "precipitation probability",
	"weatherForecast.daily.sunrise": "sunrise",
	"weatherForecast.daily.sunset": "sunset",
	"weatherForecast.wbgt.safe": "almost safe",
	"weatherForecast.wbgt.caution": "caution",
	"weatherForecast.wbgt.warning": "warning",
	"weatherForecast.wbgt.severe": "strict caution",
	"weatherForecast.wbgt.danger": "danger"
};
var ja = {
	"weatherForecast.name": "天気予報",
	"weatherForecast.getForecast": "郵便番号 [ZIP] 付近の [HOURS] 時間後の [ITEM]",
	"weatherForecast.getDailyForecast": "郵便番号 [ZIP] 付近の [DAY] 日後の [DAILY_ITEM]",
	"weatherForecast.getPlaceName": "郵便番号 [ZIP] の地名",
	"weatherForecast.item.temperature": "気温",
	"weatherForecast.item.precipitation": "降水確率",
	"weatherForecast.item.weather": "天気",
	"weatherForecast.item.windspeed": "風速",
	"weatherForecast.item.winddir": "風向き",
	"weatherForecast.item.wbgt": "WBGT(暑さ指数)",
	"weatherForecast.item.wbgtLevel": "WBGT(危険度ラベル)",
	"weatherForecast.daily.weather": "天気",
	"weatherForecast.daily.tempMax": "最高気温",
	"weatherForecast.daily.tempMin": "最低気温",
	"weatherForecast.daily.precipitation": "降水確率",
	"weatherForecast.daily.sunrise": "日の出",
	"weatherForecast.daily.sunset": "日の入り",
	"weatherForecast.wbgt.safe": "ほぼ安全",
	"weatherForecast.wbgt.caution": "注意",
	"weatherForecast.wbgt.warning": "警戒",
	"weatherForecast.wbgt.severe": "厳重警戒",
	"weatherForecast.wbgt.danger": "危険"
};
var translations = {
	en: en,
	ja: ja,
	"ja-Hira": {
	"weatherForecast.name": "てんきよほう",
	"weatherForecast.getForecast": "ゆうびんばんごう [ZIP] ふきんの [HOURS] じかんごの [ITEM]",
	"weatherForecast.getDailyForecast": "ゆうびんばんごう [ZIP] ふきんの [DAY] にちごの [DAILY_ITEM]",
	"weatherForecast.getPlaceName": "ゆうびんばんごう [ZIP] のちめい",
	"weatherForecast.item.temperature": "きおん",
	"weatherForecast.item.precipitation": "こうすいかくりつ",
	"weatherForecast.item.weather": "てんき",
	"weatherForecast.item.windspeed": "ふうそく",
	"weatherForecast.item.winddir": "かざむき",
	"weatherForecast.item.wbgt": "WBGT(あつさしすう)",
	"weatherForecast.item.wbgtLevel": "WBGT(きけんどラベル)",
	"weatherForecast.daily.weather": "てんき",
	"weatherForecast.daily.tempMax": "さいこうきおん",
	"weatherForecast.daily.tempMin": "さいていきおん",
	"weatherForecast.daily.precipitation": "こうすいかくりつ",
	"weatherForecast.daily.sunrise": "ひので",
	"weatherForecast.daily.sunset": "ひのいり",
	"weatherForecast.wbgt.safe": "ほぼあんぜん",
	"weatherForecast.wbgt.caution": "ちゅうい",
	"weatherForecast.wbgt.warning": "けいかい",
	"weatherForecast.wbgt.severe": "げんじゅうけいかい",
	"weatherForecast.wbgt.danger": "きけん"
}
};

var img = "data:image/png;base64,";

/**
 * Formatter which is used for translation.
 * This will be replaced which is used in the runtime.
 * @param {object} messageData - format-message object
 * @returns {string} - message for the locale
 */
var formatMessage = function formatMessage(messageData) {
  return messageData.default;
};

/**
 * Setup format-message for this extension.
 */
var setupTranslations = function setupTranslations() {
  var localeSetup = formatMessage.setup();
  if (localeSetup && localeSetup.translations[localeSetup.locale]) {
    Object.assign(localeSetup.translations[localeSetup.locale], translations[localeSetup.locale]);
  }
};
var EXTENSION_ID = 'weatherForecast';

/**
 * URL to get this extension as a module.
 * When it was loaded as a module, 'extensionURL' will be replaced a URL which is retrieved from.
 * @type {string}
 */
var extensionURL = 'https://asondemita.github.io/xcx-weather/dist/weatherForecast.mjs';

/**
 * Endpoint to convert a Japanese postal code into latitude/longitude.
 * @type {string}
 */
var ZIP_API = 'https://api.zippopotam.us/jp/';

/**
 * Endpoint for the Open-Meteo hourly forecast.
 * @type {string}
 */
var FORECAST_API = 'https://api.open-meteo.com/v1/forecast';

/**
 * Endpoint for the Open-Meteo geocoding search (used to localize place names).
 * @type {string}
 */
var GEOCODE_API = 'https://geocoding-api.open-meteo.com/v1/search';

/**
 * Time-to-live (ms) for the cached forecast of a location.
 * @type {number}
 */
var FORECAST_TTL = 10 * 60 * 1000;

/**
 * Number of forecast days requested from Open-Meteo. Must be large enough that
 * the largest HOUR_OPTIONS value (48h) always falls inside the returned window.
 * @type {number}
 */
var FORECAST_DAYS = 4;

/**
 * Number of days requested for the daily (weekly) forecast. Open-Meteo returns
 * today plus the following days, so this also bounds the DAY dropdown.
 * @type {number}
 */
var WEEKLY_DAYS = 7;

/**
 * Japanese labels for WMO weather interpretation codes.
 * @see https://open-meteo.com/en/docs
 * @type {Object.<number, string>}
 */
var WEATHER_CODE_JA = {
  0: '快晴',
  1: '晴れ',
  2: '晴れ時々曇り',
  3: '曇り',
  45: '霧',
  48: '霧氷',
  51: '霧雨（弱）',
  53: '霧雨',
  55: '霧雨（強）',
  56: '着氷性の霧雨（弱）',
  57: '着氷性の霧雨（強）',
  61: '雨（弱）',
  63: '雨',
  65: '雨（強）',
  66: '着氷性の雨（弱）',
  67: '着氷性の雨（強）',
  71: '雪（弱）',
  73: '雪',
  75: '雪（強）',
  77: '細氷',
  80: 'にわか雨（弱）',
  81: 'にわか雨',
  82: 'にわか雨（強）',
  85: 'にわか雪（弱）',
  86: 'にわか雪（強）',
  95: '雷雨',
  96: '雷雨（弱いひょう）',
  99: '雷雨（強いひょう）'
};

/**
 * Convert a WMO weather code into a Japanese label.
 * @param {number} code - WMO weather code
 * @returns {string} - Japanese label (or the raw code when unknown)
 */
var weatherCodeToJa = function weatherCodeToJa(code) {
  if (code === null || typeof code === 'undefined') return '';
  return WEATHER_CODE_JA[code] || "\u4E0D\u660E(".concat(code, ")");
};

/**
 * Estimate the outdoor WBGT (Wet Bulb Globe Temperature / 暑さ指数) from standard
 * meteorological values, using the regression by Ono et al. (2014). This is the
 * same formula the Japanese Ministry of the Environment uses for its WBGT
 * observations and forecasts.
 * @see https://www.wbgt.env.go.jp/wbgt_detail.php
 * @param {number} ta - air temperature (°C)
 * @param {number} rh - relative humidity (%)
 * @param {number} srWattsPerM2 - global solar (shortwave) radiation (W/m²)
 * @param {number} ws - wind speed (m/s)
 * @returns {number} - estimated WBGT (°C)
 */
var computeWbgt = function computeWbgt(ta, rh, srWattsPerM2, ws) {
  var sr = srWattsPerM2 / 1000; // formula expects kW/m²
  return 0.735 * ta + 0.0374 * rh + 0.00292 * ta * rh + 7.619 * sr - 4.557 * sr * sr - 0.0572 * ws - 4.064;
};

/**
 * WBGT danger levels (日本生気象学会「日常生活における熱中症予防指針」).
 * Ordered ascending; the first entry whose `max` exceeds the value wins, so a
 * value sitting exactly on a boundary falls into the higher (more severe) level.
 * @type {Array.<{max: number, id: string, default: string}>}
 */
var WBGT_LEVELS = [{
  max: 21,
  id: 'weatherForecast.wbgt.safe',
  default: 'almost safe'
}, {
  max: 25,
  id: 'weatherForecast.wbgt.caution',
  default: 'caution'
}, {
  max: 28,
  id: 'weatherForecast.wbgt.warning',
  default: 'warning'
}, {
  max: 31,
  id: 'weatherForecast.wbgt.severe',
  default: 'strict caution'
}, {
  max: Infinity,
  id: 'weatherForecast.wbgt.danger',
  default: 'danger'
}];

/**
 * Pick the WBGT danger level descriptor for a WBGT value.
 * @param {number} wbgt - WBGT value (°C)
 * @returns {{max: number, id: string, default: string}} - level descriptor
 */
var wbgtLevel = function wbgtLevel(wbgt) {
  return WBGT_LEVELS.find(function (level) {
    return wbgt < level.max;
  });
};

/**
 * 16-point compass labels (Japanese), starting at north and going clockwise.
 * @type {Array.<string>}
 */
var WIND_DIRECTIONS_JA = ['北', '北北東', '北東', '東北東', '東', '東南東', '南東', '南南東', '南', '南南西', '南西', '西南西', '西', '西北西', '北西', '北北西'];

/**
 * Convert a wind direction in degrees (meteorological: where the wind comes
 * from) into a 16-point Japanese compass label.
 * @param {number} deg - wind direction in degrees (0 = north)
 * @returns {string} - Japanese compass label, or '' when unavailable
 */
var windDirectionToJa = function windDirectionToJa(deg) {
  if (deg === null || typeof deg === 'undefined' || deg === '') return '';
  var idx = Math.round(Number(deg) / 22.5);
  return WIND_DIRECTIONS_JA[(idx % 16 + 16) % 16];
};

/**
 * Tolerance (ms) for matching a requested hour to an available hourly data
 * point. The grid is hourly, so any in-window request is within 30 min; a wider
 * gap means the requested time is outside the forecast window.
 * @type {number}
 */
var HOUR_MATCH_TOLERANCE_MS = 60 * 60 * 1000;

/**
 * Convert full-width ASCII characters (！-～) and the full-width space to their
 * half-width equivalents, so free-typed full-width input is accepted.
 * @param {string} raw - user input
 * @returns {string} - input with full-width forms converted to half-width
 */
var toHalfWidth = function toHalfWidth(raw) {
  return String(raw).replace(/[！-～]/g, function (c) {
    return String.fromCharCode(c.charCodeAt(0) - 0xFEE0);
  }).replace(/　/g, ' ');
};

/**
 * Parse a freely-typed number, accepting half-width or full-width digits/signs
 * (e.g. "3", "１２", "－2").
 * @param {string} raw - user input
 * @returns {number} - parsed number, or NaN when it is not numeric
 */
var parseLooseNumber = function parseLooseNumber(raw) {
  var text = toHalfWidth(raw).trim();
  if (text === '') return NaN;
  var value = Number(text);
  return Number.isFinite(value) ? value : NaN;
};

/**
 * Normalize a Japanese postal code into the "NNN-NNNN" form expected by the API.
 * Accepts half-width or full-width digits, with or without a hyphen
 * (e.g. "1000001", "100-0001", "１０００００１", "１００－０００１").
 * @param {string} raw - user input
 * @returns {?string} - normalized code, or null when it is not 7 digits
 */
var normalizeZip = function normalizeZip(raw) {
  var digits = toHalfWidth(raw).replace(/[^0-9]/g, '');
  if (digits.length !== 7) return null;
  return "".concat(digits.slice(0, 3), "-").concat(digits.slice(3));
};

/**
 * Scratch 3.0 blocks to get a weather forecast from Open-Meteo.
 */
var ExtensionBlocks = /*#__PURE__*/function () {
  /**
   * Construct a set of blocks for 天気予報.
   * @param {Runtime} runtime - the Scratch 3.0 runtime.
   */
  function ExtensionBlocks(runtime) {
    _classCallCheck(this, ExtensionBlocks);
    /**
     * The Scratch 3.0 runtime.
     * @type {Runtime}
     */
    this.runtime = runtime;
    if (runtime.formatMessage) {
      // Replace 'formatMessage' to a formatter which is used in the runtime.
      formatMessage = runtime.formatMessage;
    }

    /**
     * Cache of postal-code -> {latitude, longitude} lookups.
     * @type {Object.<string, Promise<?object>>}
     */
    this._geoCache = {};

    /**
     * Cache of location -> {time, data} forecast results.
     * @type {Object.<string, {fetchedAt: number, data: Promise<?object>}>}
     */
    this._forecastCache = {};

    /**
     * Cache of location -> {time, data} daily (weekly) forecast results.
     * @type {Object.<string, {fetchedAt: number, data: Promise<?object>}>}
     */
    this._dailyCache = {};

    /**
     * Cache of coordinates -> Promise<string> Japanese place name lookups.
     * @type {Object.<string, Promise<string>>}
     */
    this._nameCache = {};
  }

  /**
   * @returns {object} metadata for this extension and its blocks.
   */
  return _createClass(ExtensionBlocks, [{
    key: "getInfo",
    value: function getInfo() {
      setupTranslations();
      return {
        id: ExtensionBlocks.EXTENSION_ID,
        name: ExtensionBlocks.EXTENSION_NAME,
        extensionURL: ExtensionBlocks.extensionURL,
        blockIconURI: img,
        showStatusButton: false,
        blocks: [{
          opcode: 'getForecast',
          blockType: BlockType.REPORTER,
          blockAllThreads: false,
          text: formatMessage({
            id: 'weatherForecast.getForecast',
            default: 'forecast [ITEM] in [HOURS] hours near zip [ZIP]',
            description: 'get a weather forecast value'
          }),
          func: 'getForecast',
          arguments: {
            ITEM: {
              type: ArgumentType.STRING,
              menu: 'itemMenu',
              defaultValue: 'weather'
            },
            HOURS: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            },
            ZIP: {
              type: ArgumentType.STRING,
              defaultValue: '100-0001'
            }
          }
        }, {
          opcode: 'getDailyForecast',
          blockType: BlockType.REPORTER,
          blockAllThreads: false,
          text: formatMessage({
            id: 'weatherForecast.getDailyForecast',
            default: 'weekly [DAILY_ITEM] in [DAY] days near zip [ZIP]',
            description: 'get a daily (weekly) weather forecast value'
          }),
          func: 'getDailyForecast',
          arguments: {
            DAILY_ITEM: {
              type: ArgumentType.STRING,
              menu: 'dailyItemMenu',
              defaultValue: 'weather'
            },
            DAY: {
              type: ArgumentType.NUMBER,
              defaultValue: 1
            },
            ZIP: {
              type: ArgumentType.STRING,
              defaultValue: '100-0001'
            }
          }
        }, {
          opcode: 'getPlaceName',
          blockType: BlockType.REPORTER,
          blockAllThreads: false,
          text: formatMessage({
            id: 'weatherForecast.getPlaceName',
            default: 'place name of zip [ZIP]',
            description: 'get the resolved place name for a postal code'
          }),
          func: 'getPlaceName',
          arguments: {
            ZIP: {
              type: ArgumentType.STRING,
              defaultValue: '100-0001'
            }
          }
        }],
        menus: {
          itemMenu: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'weatherForecast.item.weather',
                default: 'weather',
                description: 'weather menu item'
              }),
              value: 'weather'
            }, {
              text: formatMessage({
                id: 'weatherForecast.item.temperature',
                default: 'temperature',
                description: 'temperature menu item'
              }),
              value: 'temperature'
            }, {
              text: formatMessage({
                id: 'weatherForecast.item.precipitation',
                default: 'precipitation probability',
                description: 'precipitation probability menu item'
              }),
              value: 'precipitation'
            }, {
              text: formatMessage({
                id: 'weatherForecast.item.windspeed',
                default: 'wind speed',
                description: 'wind speed menu item'
              }),
              value: 'windspeed'
            }, {
              text: formatMessage({
                id: 'weatherForecast.item.winddir',
                default: 'wind direction',
                description: 'wind direction menu item'
              }),
              value: 'winddir'
            }, {
              text: formatMessage({
                id: 'weatherForecast.item.wbgt',
                default: 'heat index (WBGT)',
                description: 'WBGT value menu item'
              }),
              value: 'wbgt'
            }, {
              text: formatMessage({
                id: 'weatherForecast.item.wbgtLevel',
                default: 'heat risk level (WBGT)',
                description: 'WBGT danger level menu item'
              }),
              value: 'wbgtLevel'
            }]
          },
          dailyItemMenu: {
            acceptReporters: true,
            items: [{
              text: formatMessage({
                id: 'weatherForecast.daily.weather',
                default: 'weather',
                description: 'daily weather menu item'
              }),
              value: 'weather'
            }, {
              text: formatMessage({
                id: 'weatherForecast.daily.tempMax',
                default: 'highest temperature',
                description: 'daily max temperature menu item'
              }),
              value: 'tempMax'
            }, {
              text: formatMessage({
                id: 'weatherForecast.daily.tempMin',
                default: 'lowest temperature',
                description: 'daily min temperature menu item'
              }),
              value: 'tempMin'
            }, {
              text: formatMessage({
                id: 'weatherForecast.daily.precipitation',
                default: 'precipitation probability',
                description: 'daily precipitation probability menu item'
              }),
              value: 'precipitation'
            }, {
              text: formatMessage({
                id: 'weatherForecast.daily.sunrise',
                default: 'sunrise',
                description: 'daily sunrise time menu item'
              }),
              value: 'sunrise'
            }, {
              text: formatMessage({
                id: 'weatherForecast.daily.sunset',
                default: 'sunset',
                description: 'daily sunset time menu item'
              }),
              value: 'sunset'
            }]
          }
        }
      };
    }

    /**
     * Look up latitude/longitude for a postal code (memoized).
     * @param {string} zip - normalized "NNN-NNNN" postal code
     * @returns {Promise<?{latitude: number, longitude: number}>} - coordinates or null
     */
  }, {
    key: "_lookupLocation",
    value: function _lookupLocation(zip) {
      if (this._geoCache[zip]) return this._geoCache[zip];
      var request = fetch("".concat(ZIP_API).concat(zip)).then(function (res) {
        return res.ok ? res.json() : null;
      }).then(function (json) {
        if (!json || !json.places || json.places.length === 0) return null;
        var place = json.places[0];
        // Zippopotam returns Japanese names in romaji (e.g. "Chiyoda", "Toukyouto").
        var placeName = place['place name'] || '';
        var state = place.state || '';
        var name = [state, placeName].filter(Boolean).join(' ');
        return {
          latitude: Number(place.latitude),
          longitude: Number(place.longitude),
          placeName: placeName,
          state: state,
          name: name
        };
      }).catch(function () {
        return null;
      });
      this._geoCache[zip] = request;
      return request;
    }

    /**
     * Fetch the hourly forecast for a location (memoized with a short TTL).
     * @param {{latitude: number, longitude: number}} location - coordinates
     * @returns {Promise<?object>} - Open-Meteo hourly response or null
     */
  }, {
    key: "_fetchForecast",
    value: function _fetchForecast(location) {
      var key = "".concat(location.latitude.toFixed(3), ",").concat(location.longitude.toFixed(3));
      var cached = this._forecastCache[key];
      var now = Date.now();
      if (cached && now - cached.fetchedAt < FORECAST_TTL) {
        return cached.data;
      }
      var params = new URLSearchParams({
        latitude: String(location.latitude),
        longitude: String(location.longitude),
        hourly: 'temperature_2m,relative_humidity_2m,precipitation_probability,' + 'weather_code,wind_speed_10m,wind_direction_10m,shortwave_radiation',
        wind_speed_unit: 'ms',
        timezone: 'Asia/Tokyo',
        forecast_days: String(FORECAST_DAYS)
      });
      var request = fetch("".concat(FORECAST_API, "?").concat(params.toString())).then(function (res) {
        return res.ok ? res.json() : null;
      }).catch(function () {
        return null;
      });
      this._forecastCache[key] = {
        fetchedAt: now,
        data: request
      };
      return request;
    }

    /**
     * Fetch the daily (weekly) forecast for a location (memoized with a short TTL).
     * @param {{latitude: number, longitude: number}} location - coordinates
     * @returns {Promise<?object>} - Open-Meteo daily response or null
     */
  }, {
    key: "_fetchDailyForecast",
    value: function _fetchDailyForecast(location) {
      var key = "".concat(location.latitude.toFixed(3), ",").concat(location.longitude.toFixed(3));
      var cached = this._dailyCache[key];
      var now = Date.now();
      if (cached && now - cached.fetchedAt < FORECAST_TTL) {
        return cached.data;
      }
      var params = new URLSearchParams({
        latitude: String(location.latitude),
        longitude: String(location.longitude),
        daily: 'weather_code,temperature_2m_max,temperature_2m_min,' + 'precipitation_probability_max,sunrise,sunset',
        timezone: 'Asia/Tokyo',
        forecast_days: String(WEEKLY_DAYS)
      });
      var request = fetch("".concat(FORECAST_API, "?").concat(params.toString())).then(function (res) {
        return res.ok ? res.json() : null;
      }).catch(function () {
        return null;
      });
      this._dailyCache[key] = {
        fetchedAt: now,
        data: request
      };
      return request;
    }

    /**
     * Resolve a Japanese place name for a location (memoized). Zippopotam only
     * returns romaji, so we search Open-Meteo geocoding (language=ja) by the
     * romaji name and pick the candidate closest to the known coordinates.
     * Falls back to the romaji name when geocoding finds nothing.
     * @param {{latitude: number, longitude: number, placeName: string, name: string}} location
     *     - resolved location with its romaji name
     * @returns {Promise<string>} - Japanese place name (or romaji fallback)
     */
  }, {
    key: "_lookupJapaneseName",
    value: function _lookupJapaneseName(location) {
      var key = "".concat(location.latitude.toFixed(3), ",").concat(location.longitude.toFixed(3));
      if (this._nameCache[key]) return this._nameCache[key];
      var fallback = location.name || '';
      if (!location.placeName) {
        this._nameCache[key] = Promise.resolve(fallback);
        return this._nameCache[key];
      }
      var params = new URLSearchParams({
        name: location.placeName,
        count: '10',
        language: 'ja',
        format: 'json'
      });
      var request = fetch("".concat(GEOCODE_API, "?").concat(params.toString())).then(function (res) {
        return res.ok ? res.json() : null;
      }).then(function (json) {
        var results = json && json.results || [];
        var jp = results.filter(function (r) {
          return r.country_code === 'JP';
        });
        var pool = jp.length ? jp : results;
        if (pool.length === 0) return fallback;
        // Multiple places can share a romaji name; pick the nearest.
        var best = pool[0];
        var bestDiff = Infinity;
        pool.forEach(function (r) {
          var dLat = Number(r.latitude) - location.latitude;
          var dLon = Number(r.longitude) - location.longitude;
          var diff = dLat * dLat + dLon * dLon;
          if (diff < bestDiff) {
            bestDiff = diff;
            best = r;
          }
        });
        var name = [best.admin1, best.name].filter(Boolean).join('');
        return name || fallback;
      }).catch(function () {
        return fallback;
      });
      this._nameCache[key] = request;
      return request;
    }

    /**
     * Find the index in the hourly time array that is closest to "now + hours".
     * Timezone-robust: uses the offset returned by the API rather than the
     * browser's local timezone.
     * @param {object} forecast - Open-Meteo response
     * @param {number} hours - hours ahead of the current time
     * @returns {{index: number, diffMs: number}} - nearest index and how far
     *     (ms) that data point is from the requested time
     */
  }, {
    key: "_indexForHoursAhead",
    value: function _indexForHoursAhead(forecast, hours) {
      var times = forecast.hourly.time;
      var offsetMs = (forecast.utc_offset_seconds || 0) * 1000;
      var targetMs = Date.now() + hours * 60 * 60 * 1000;
      var bestIndex = 0;
      var bestDiff = Infinity;
      for (var i = 0; i < times.length; i++) {
        // The time strings are local wall-clock; convert back to a real instant.
        var instant = Date.parse("".concat(times[i], ":00Z")) - offsetMs;
        var diff = Math.abs(instant - targetMs);
        if (diff < bestDiff) {
          bestDiff = diff;
          bestIndex = i;
        }
      }
      return {
        index: bestIndex,
        diffMs: bestDiff
      };
    }

    /**
     * Report a forecast value for a postal code at a given hour offset.
     * @param {object} args - block arguments
     * @param {string} args.ITEM - one of temperature/precipitation/weather/windspeed
     * @param {string} args.HOURS - hours ahead of now (free input, full-width ok)
     * @param {string} args.ZIP - Japanese postal code
     * @returns {Promise<(string|number)>} - the requested value, or '' on failure
     */
  }, {
    key: "getForecast",
    value: function getForecast(args) {
      var _this = this;
      var item = Cast.toString(args.ITEM);
      var hours = parseLooseNumber(args.HOURS);
      var zip = normalizeZip(args.ZIP);
      // Reject non-numeric or past times; out-of-window times are caught below.
      if (!zip || Number.isNaN(hours) || hours < 0) return Promise.resolve('');
      return this._lookupLocation(zip).then(function (location) {
        if (!location) return '';
        return _this._fetchForecast(location).then(function (forecast) {
          if (!forecast || !forecast.hourly || !forecast.hourly.time) return '';
          var match = _this._indexForHoursAhead(forecast, hours);
          // Outside the available forecast window -> no misleading value.
          if (match.diffMs > HOUR_MATCH_TOLERANCE_MS) return '';
          var i = match.index;
          var hourly = forecast.hourly;
          switch (item) {
            case 'temperature':
              {
                var v = hourly.temperature_2m && hourly.temperature_2m[i];
                return v === null || typeof v === 'undefined' ? '' : v;
              }
            case 'precipitation':
              {
                var _v = hourly.precipitation_probability && hourly.precipitation_probability[i];
                return _v === null || typeof _v === 'undefined' ? '' : _v;
              }
            case 'windspeed':
              {
                var _v2 = hourly.wind_speed_10m && hourly.wind_speed_10m[i];
                return _v2 === null || typeof _v2 === 'undefined' ? '' : _v2;
              }
            case 'winddir':
              {
                var _v3 = hourly.wind_direction_10m && hourly.wind_direction_10m[i];
                return windDirectionToJa(_v3);
              }
            case 'weather':
              {
                var _v4 = hourly.weather_code && hourly.weather_code[i];
                return weatherCodeToJa(_v4);
              }
            case 'wbgt':
            case 'wbgtLevel':
              {
                var ta = hourly.temperature_2m && hourly.temperature_2m[i];
                var rh = hourly.relative_humidity_2m && hourly.relative_humidity_2m[i];
                var sr = hourly.shortwave_radiation && hourly.shortwave_radiation[i];
                var ws = hourly.wind_speed_10m && hourly.wind_speed_10m[i];
                var missing = [ta, rh, sr, ws].some(function (v) {
                  return v === null || typeof v === 'undefined';
                });
                if (missing) return '';
                var wbgt = computeWbgt(ta, rh, sr, ws);
                if (item === 'wbgtLevel') {
                  var level = wbgtLevel(wbgt);
                  return formatMessage({
                    id: level.id,
                    default: level.default,
                    description: 'WBGT danger level'
                  });
                }
                return Math.round(wbgt * 10) / 10;
              }
            default:
              return '';
          }
        });
      }).catch(function () {
        return '';
      });
    }

    /**
     * Report a daily (weekly) forecast value for a postal code on a given day.
     * @param {object} args - block arguments
     * @param {string} args.DAILY_ITEM - one of weather/tempMax/tempMin/precipitation
     * @param {string} args.DAY - days ahead of today (0 = today, free input, full-width ok)
     * @param {string} args.ZIP - Japanese postal code
     * @returns {Promise<(string|number)>} - the requested value, or '' on failure
     */
  }, {
    key: "getDailyForecast",
    value: function getDailyForecast(args) {
      var _this2 = this;
      var item = Cast.toString(args.DAILY_ITEM);
      var dayValue = parseLooseNumber(args.DAY);
      var day = Math.round(dayValue);
      var zip = normalizeZip(args.ZIP);
      if (!zip || Number.isNaN(dayValue)) return Promise.resolve('');
      return this._lookupLocation(zip).then(function (location) {
        if (!location) return '';
        return _this2._fetchDailyForecast(location).then(function (forecast) {
          if (!forecast || !forecast.daily || !forecast.daily.time) return '';
          var daily = forecast.daily;
          if (day < 0 || day >= daily.time.length) return '';
          switch (item) {
            case 'weather':
              {
                var v = daily.weather_code && daily.weather_code[day];
                return weatherCodeToJa(v);
              }
            case 'tempMax':
              {
                var _v5 = daily.temperature_2m_max && daily.temperature_2m_max[day];
                return _v5 === null || typeof _v5 === 'undefined' ? '' : _v5;
              }
            case 'tempMin':
              {
                var _v6 = daily.temperature_2m_min && daily.temperature_2m_min[day];
                return _v6 === null || typeof _v6 === 'undefined' ? '' : _v6;
              }
            case 'precipitation':
              {
                var _v7 = daily.precipitation_probability_max && daily.precipitation_probability_max[day];
                return _v7 === null || typeof _v7 === 'undefined' ? '' : _v7;
              }
            case 'sunrise':
            case 'sunset':
              {
                var series = item === 'sunrise' ? daily.sunrise : daily.sunset;
                var _v8 = series && series[day];
                if (_v8 === null || typeof _v8 === 'undefined') return '';
                // ISO8601 like "2026-06-15T04:25" -> "04:25".
                return String(_v8).split('T')[1] || '';
              }
            default:
              return '';
          }
        });
      }).catch(function () {
        return '';
      });
    }

    /**
     * Report the Japanese place name that a postal code resolves to. This makes
     * it clear that the forecast is for the area around the code (one
     * representative point), not an exact pinpoint, e.g. "東京都千代田区".
     * @param {object} args - block arguments
     * @param {string} args.ZIP - Japanese postal code
     * @returns {Promise<string>} - resolved place name, or '' on failure
     */
  }, {
    key: "getPlaceName",
    value: function getPlaceName(args) {
      var _this3 = this;
      var zip = normalizeZip(args.ZIP);
      if (!zip) return Promise.resolve('');
      return this._lookupLocation(zip).then(function (location) {
        return location ? _this3._lookupJapaneseName(location) : '';
      }).catch(function () {
        return '';
      });
    }
  }], [{
    key: "formatMessage",
    set:
    /**
     * A translation object which is used in this class.
     * @param {FormatObject} formatter - translation object
     */
    function set(formatter) {
      formatMessage = formatter;
      if (formatMessage) setupTranslations();
    }

    /**
     * @return {string} - the name of this extension.
     */
  }, {
    key: "EXTENSION_NAME",
    get: function get() {
      return formatMessage({
        id: 'weatherForecast.name',
        default: '天気予報',
        description: 'name of the extension'
      });
    }

    /**
     * @return {string} - the ID of this extension.
     */
  }, {
    key: "EXTENSION_ID",
    get: function get() {
      return EXTENSION_ID;
    }

    /**
     * URL to get this extension.
     * @type {string}
     */
  }, {
    key: "extensionURL",
    get: function get() {
      return extensionURL;
    }

    /**
     * Set URL to get this extension.
     * The extensionURL will be changed to the URL of the loading server.
     * @param {string} url - URL
     */,
    set: function set(url) {
      extensionURL = url;
    }
  }]);
}();

export { ExtensionBlocks as blockClass, computeWbgt, entry, normalizeZip, parseLooseNumber, wbgtLevel, weatherCodeToJa, windDirectionToJa };
//# sourceMappingURL=weatherForecast.mjs.map
